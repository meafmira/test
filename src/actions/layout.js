import { createActions } from 'redux-actions'
import LayoutConstants from '../constants/layout'

const { SHOW_FINANCING_OPTIONS } = LayoutConstants

export default createActions(SHOW_FINANCING_OPTIONS)
